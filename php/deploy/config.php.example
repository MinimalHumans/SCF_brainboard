<?php
// php/deploy/config.php - DO NOT COMMIT THIS FILE IF IT CONTAINS REAL SECRETS
// On your server, create this file manually or via your host's file manager.

define('DEPLOY_SECRET', 'your_super_secret_hash_here');

// Optional: override default target directory
// define('DEPLOY_TARGET', '/home/user/public_html/');

// --- Dev environment (hit this script with ?env=dev) ---
// Uses a fixed release tag instead of /releases/latest so a dev deploy never
// depends on (or clobbers) whatever the current prod release tag is.
define('DEPLOY_SECRET_DEV', 'a_different_super_secret_hash_here');
define('DEPLOY_TARGET_DEV', '/home/user/dev.example.com/');
// define('DEPLOY_RELEASE_TAG_DEV', 'dev'); // default is 'dev'
